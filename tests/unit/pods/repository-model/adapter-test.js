import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:repository-model', 'Unit | Adapter | repository model', {
});

test('it exists', function(assert) {
  let adapter = this.subject();
  assert.ok(adapter);
});
